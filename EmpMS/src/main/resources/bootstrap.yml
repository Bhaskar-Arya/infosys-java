server:
 port: 9091
spring: 
 application:
  name: EMP
 cloud:  
  consul:
   discovery: 
    hostname: localhost
   config:
    enabled: true
    prefixes: config
    default-context: application
    profile-separator: '::'
    format: yaml
   