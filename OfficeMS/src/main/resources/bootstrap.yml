server:
 port: 9092
spring: 
  application:
    name: Office
  cloud:
   consul:
    discovery: 
     hostname: localhost
    config:
     enabled: true
     prefixes: config
     default-context: application
     profile-separator: '::'
     format: yaml
   